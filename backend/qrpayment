const QRCode = require('qrcode');

const imageUrl = 'https://bit.ly/4hL5pXh'; // Shortened URL that redirects to the image

QRCode.toFile('qrcode.png', imageUrl, (err) => {
    if (err) throw err;
    console.log('QR Code linking to the image successfully generated!');
});
